/** Component key the missing-field projection is compiled and rendered under. */
export const MISSING_SCHEMA_NAME = 'missing';

/** Matches the 1 MiB standard-input ceiling enforced by the agent process. */
export const MISSING_PROMPT_LIMIT_BYTES = 1024 * 1024;
