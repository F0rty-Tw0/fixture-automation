/** Directory the CLIs write into when no out-dir is given. */
export const DEFAULT_OUT_DIR = 'fixtures';

/** Subdirectory of the out-dir receiving missing.json, missing.d.ts and missing.stub.ts. */
export const MISSING_DIR = 'missing';
